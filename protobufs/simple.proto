syntax = "proto2";

message EchoRequest {
  optional bytes request_text = 1;
}

message QuitRequest {}

message GeneralReply {
  optional bytes reply_text = 1;
}

service SimpleService {
  rpc Echo (EchoRequest) returns (GeneralReply);
  rpc Quit (QuitRequest) returns (GeneralReply);
}
